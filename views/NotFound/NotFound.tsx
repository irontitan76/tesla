import { NotFoundContents } from './NotFoundContents';

export const metadata = {
  title: 'Not Found',
};

export const NotFound = () => {
  return (
    <NotFoundContents />
  );
};

export default NotFound;
