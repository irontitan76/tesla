import React from 'react';
import { EnterContents } from './EnterContents';

export const metadata = {
  title: 'Enter',
};

export const Enter = () => {
  return (
    <EnterContents />
  );
};

export default Enter;
