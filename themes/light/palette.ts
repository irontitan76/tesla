import { common } from 'themes/common/palette';

export const background = {
  default: '#fff',
  card: '#eee',
  paper: '#eeeeee',
};

export const divider = 'rgba(0,0,0,0.12)';

export const text = {
  primary: `${common.black}bb`,
  secondary: `${common.black}99`,
};

export const palette = {
  background,
  divider,
  text,
};
